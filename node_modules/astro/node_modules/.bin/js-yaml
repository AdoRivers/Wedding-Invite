#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../js-yaml/bin/js-yaml.js" "$@"
else
  exec node  "$basedir/../../../js-yaml/bin/js-yaml.js" "$@"
fi
# cmd-shim-target=C:/Users/Fran/Documents/Proyectos/BIrthday-invite/node_modules/.pnpm/astro@6.2.2_jiti@2.6.1_lightningcss@1.32.0_rollup@4.60.3/node_modules/js-yaml/bin/js-yaml.js
